# OpenRouter Usage 설치형 완성 설계

2026-09-13. 이 문서는 기존 DESIGN.md의 호스트 패치 의존 배포 방식을 대체하는 후속 설계다. 구현 담당은 dev 워크트리의 Grok 세션이다.

## 목표와 화면 결정

사용자는 플러그인을 Orca에 추가하고 활성화한 뒤 사용량을 확인할 수 있어야 한다. 별도의 npm 서버 실행, 사용자 Orca 바이너리 수정, 수동 host-patch 적용은 설치 과정에 포함하지 않는다.

현재 확인한 Orca 1.4.198의 공개 패널 bridge에는 워커 요청 기능이 없다. 기존 Orca에 설치하는 경로를 우선하여, **플러그인의 OpenRouter: Open Dashboard 명령 → Orca 내부 브라우저 탭**을 목표 UX로 삼는다. 사이드바 iframe 안의 라이브 대시보드는 이 경로의 제공 기능으로 표기하지 않는다. 사용자가 사이드바 전용을 선택하면 별도 Orca 소스 빌드 경로로 설계를 변경해야 한다.

## 사용자 흐름

1. 빌드 산출물이 포함된 플러그인 폴더를 Settings → Plugins → Development에 추가하고 활성화한다.
2. Orca 명령 팔레트에서 OpenRouter: Open Dashboard를 실행한다.
3. 플러그인이 필요한 로컬 서비스를 시작하고 Orca 브라우저 탭을 연다. 사용자가 터미널을 별도로 실행할 필요가 없어야 한다.
4. 첫 실행에는 관리 키 등록 화면을 보여 준다. 검증 성공 후 Orca secrets에 저장한다.
5. 7/30일 요약·그래프·모델/공급자 상세·새로고침·연결 해제를 제공한다.
6. 다시 실행하면 저장된 연결을 복원한다. 이미 열려 있는 탭은 가능한 경우 재사용한다.

패널을 남기는 경우 실행할 명령을 짧게 안내한다. 작동하지 않는 Refresh/Settings 버튼, 지원되지 않는 postMessage 요청, Host bridge required 오류 화면을 기본 UX로 남기지 않는다. 패널에서 명령 호출도 지원된다고 가정하지 않는다.

## 기존 기능으로 구성하는 실행 경로

`manifest command → Node 플러그인 워커 → loopback HTTP service → Orca browser tab`

기존 UsageService·OrcaStore·집계 및 UI를 재사용하되, 실제 제품용 브라우저 페이지와 샌드박스 패널을 구분한다. 제품 페이지는 같은 origin의 제한된 JSON API로 워커와 통신하며 API 키를 OpenRouter로 보내는 주체는 워커다. 기존 sandbox panel의 connect-src none 정책을 완화하지 않는다.

Orca CLI의 `tab create --url ... --worktree ... --json`을 이용한다. Node에서 실행 파일과 인자 배열을 분리하여 호출하고 `shell: true`를 사용하지 않는다. 워커는 Orca 터미널 환경을 자동 상속하지 않으므로 실행 파일과 대상 워크트리 해석을 먼저 검증한다. Linux의 bare `orca`가 GNOME 화면 읽기 프로그램일 수 있음을 고려하여 검증된 Orca CLI 경로를 사용한다. 실제 호스트에서 얻은 워크트리 선택자를 사용하고 현재 저장소 경로를 배포 코드에 하드코딩하지 않는다.

## 구현 전 필수 수직 검증

첫 단계에서 최소한의 실제 플러그인 command로 다음을 확인한다. 이 검증이 실패하면 대시보드 완성을 주장하지 말고 원인과 필요한 제품 선택을 명확히 보고한다.

- 승인된 실제 플러그인 워커에서 로컬 HTTP 서비스 시작 및 CLI 호출 가능 여부.
- 워커에서 대상 Orca 브라우저/워크트리를 알아내고 정확히 여는 방법. 호스트의 workspace.readContext만으로 경로/ID가 나온다고 가정하지 않는다.
- 사용자/플러그인 관리 경로 모두에서 명령이 처리되는 실제 동작.
- 워커 유휴 회수·앱 종료·플러그인 비활성화 시 서버 수명. 단순 Node HTTP 리스너만으로 워커가 유지된다고 가정하지 않는다.
- 브라우저에서 새로고침·재실행·워커 종료 후 재연결 동작. 수명 문제를 숨기기 위해 분리된 프로세스를 영구 실행하지 않는다.

공개 CLI로 브라우저를 열 수 없거나 worker 수명을 정상 관리할 수 없다면, 설치된 Orca 파일을 수정하는 우회로 전환하지 않는다. 실패 근거를 기록하고 확보 가능한 실행 흐름을 제시한다.

## 인증·로컬 서비스

- 127.0.0.1에 동적 포트로 bind하고 한 워커의 중복 실행을 합친다. 공개 인터페이스에 listen하지 않는다.
- localhost라는 이유만으로 요청을 신뢰하지 않는다. Host 검증, 제품 origin의 Origin 검증, 세션 인증, JSON content type, 요청 크기 제한을 적용한다. 모든 조회와 변경 요청에 인증을 적용한다. 임의 origin의 CORS를 허용하지 않는다.
- 브라우저 실행에는 관리 키 대신 짧은 수명의 일회성 진입 토큰을 사용한다. 제품 세션으로 교환하고 주소/브라우저 기록에서 제거한다. management key는 URL, 명령 인자, 로그, 정적 HTML, localStorage, API 응답에 포함하지 않는다.
- 비밀 저장은 Orca secrets만 사용한다. 암호화 오류 시 평문 저장으로 전환하지 않는다.
- 개발 검증에는 사용자 승인된 .env의 OPENROUTER_MANAGEMENT_KEY를 직접 로딩한다. OPENROUTER_API_KEY는 Codex 추론용으로 별개다. 값은 모델 컨텍스트와 도구 출력에 노출하지 않는다.
- 실제 제품은 사용자가 UI에서 등록하는 키로도 동작해야 한다. 개발자의 절대 .env 경로에 의존하지 않는다.
- 비활성화/종료 시 서버·진입 토큰·세션을 정리한다. 관리 키 삭제와 서버 종료는 분리하여 재실행 시 저장한 연결이 유지되게 한다.

## 데이터·경합 마무리

기존 리뷰 수정 889de51은 시작점이다. 동시 요청과 저장/삭제의 모든 await 경계를 다시 검사한다.

- 7/30일 요청은 원본 HTTP 결과를 공유하고 집계는 요청별로 한다. 기존 기간별 dedupe가 별도 HTTP를 발생시키는 부분을 정리한다.
- UI에서 오래된 조회 응답이 새 기간 선택이나 연결 해제 상태를 덮지 않도록 요청 세대를 사용한다.
- UTC 날짜가 바뀌면 새 데이터를 조회한다. 조회 실패 시 오래된 캐시는 실제 fetchedAt 당시의 완료 일 범위를 유지한다. 새 날짜를 0 사용량으로 만들어 붙이지 않는다.
- 키 저장/삭제와 캐시 기록을 직렬화하고 연결 세대 검사로 오래된 작업의 반영을 막는다. 같은 키를 재등록하는 경우에도 오래된 정리 작업이 새 키를 지우지 않아야 한다.
- BYOK/추론 중복 합산 금지, 빈 응답과 오류 구분, timeout과 rate limit 처리, 저장·전송 크기 제한을 유지한다.

## 배포와 완료 기준

기존 host-patch는 향후 사이드바 통합을 위한 참고 자료로만 두고 필수 설치 단계에서 제거한다. dist에 필요한 실행 파일을 모두 포함하며 별도 npm install 없이 Orca가 워커를 로딩할 수 있어야 한다. README에는 개발 preview와 실제 설치형 경로를 분명히 구분한다.

완료 보고에는 다음의 실제 증거가 필요하다.

1. 깨끗한 배포 폴더를 Orca Development에 추가하고 승인된 플러그인 명령을 실행한 결과.
2. 그 명령으로 Orca 브라우저가 열리고 관리 키로 실제 Activity GET이 성공한 화면. 가짜 데이터나 별도 preview 서버는 이 증거로 인정하지 않는다.
3. 키 등록/삭제, 7/30일 변경, 새로고침, 재실행/재시작을 확인한 결과.
4. CSP 유지, 인증 없는 로컬 API 접근 차단, 키 비노출, UTC 경계·동시 조회·연결 경합 회귀 테스트.
5. npm test, TypeScript 검사, 빌드와 산출물 일치, 커밋 비밀 검사.

구현은 dev에서 수행한다. PR #1은 이미 main에 병합되었다. 원격 main을 기준으로 dev를 갱신하고 별도 후속 PR을 준비한다. main에 자동 병합하거나 설치된 Orca를 교체하지 않는다. main 워크트리의 설계 변경과 사용자의 .env를 보존한다.
